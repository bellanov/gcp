# This workflow installs the latest version of Terraform CLI and authenticates with Google Cloud using GOOGLE_APPLICATION_CREDENTIALS.
# On pull request events, this workflow will run `terraform init`, `terraform fmt`, and `terraform plan`.
# On push events to the "main" branch, `terraform apply` will be executed.
#
# Documentation for `hashicorp/setup-terraform` is located here: https://github.com/hashicorp/setup-terraform
#
# To use this workflow, you will need to complete the following setup steps.
#
# 1. Create a `main.tf` file in the root of this repository with the GCS backend and one or more resources defined.
#   Example `main.tf`:
#     # The configuration for the `gcs` backend.
#     terraform {
#       backend "gcs" {
#         bucket = "my-terraform-state-bucket"
#         prefix = "prod"
#       }
#     }
#
#     # Example GCP resource.
#     resource "google_compute_instance" "example" {
#       name         = "my-instance"
#       machine_type = "n1-standard-1"
#       zone         = "us-central1-a"
#     }
#
#
# 2. Create a Google Cloud Service Account with appropriate Terraform permissions and store its JSON key as a GitHub secret (e.g. GCP_SA_KEY).
#   Documentation:
#     - https://cloud.google.com/docs/authentication/getting-started
#     - https://help.github.com/en/actions/configuring-and-managing-workflows/creating-and-storing-encrypted-secrets
#
# 3. The workflow will automatically use GOOGLE_APPLICATION_CREDENTIALS environment variable for GCP authentication.
#   The service account key should be stored as a GitHub secret named GCP_SA_KEY.

name: "🚀 CD: Deployment"

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  terraform:
    name: "Terraform"
    runs-on: ubuntu-latest
    environment: development

    env:
      GOOGLE_APPLICATION_CREDENTIALS: /tmp/gcp-key.json

    # Use the Bash shell regardless whether the GitHub Actions runner is ubuntu-latest, macos-latest, or windows-latest
    defaults:
      run:
        shell: bash
        working-directory: terraform

    steps:
      # Checkout the repository to the GitHub Actions runner
      - name: Checkout
        uses: actions/checkout@v7

      # Authenticate with Google Cloud
      - name: Authenticate to Google Cloud
        run: |
          echo "${{ secrets.GCP_SA_KEY }}" > /tmp/gcp-key.json
          chmod 600 /tmp/gcp-key.json

      # Install the latest version of Terraform CLI
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4

      # Initialize a new or existing Terraform working directory by creating initial files, loading any remote state, downloading modules, etc.
      - name: Terraform Init
        run: terraform init

      # Checks that all Terraform configuration files adhere to a canonical format
      - name: Terraform Format
        run: terraform fmt -check

      # Generates an execution plan for Terraform
      - name: Terraform Plan
        run: terraform plan -input=false

      # On push to "main", build or change infrastructure according to Terraform configuration files
      # Note: It is recommended to set up a required "strict" status check in your repository for "Terraform Cloud". See the documentation on "strict" required status checks for more information: https://github.com/en/github/administering-a-repository/types-of-required-status-checks
      - name: Terraform Apply
        if: github.ref == 'refs/heads/"main"' && github.event_name == 'push'
        run: terraform apply -auto-approve -input=false

      # Clean up credentials file
      - name: Cleanup credentials
        if: always()
        run: rm -f /tmp/gcp-key.json
